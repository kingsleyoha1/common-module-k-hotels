{
  "name": "@kingsley555/common-module-k-hotels",
  "version": "1.0.01",
  "description": "common modules for k hotels project ",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "repository": "git@github.com:kingsleyoha1/common-module-k-hotels.git",
  "author": "Kingsley Oha",
  "license": "MIT",
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && yarn version patch && yarn run build && yarn publish --new-version 1.0.02 "
  },
  "dependencies": {
    "del-cli": "^5.1.0",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/cookie-session": "^2.0.48",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.5",
    "cookie-session": "^2.0.0",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "node-nats-streaming": "^0.3.2"
  }
}
